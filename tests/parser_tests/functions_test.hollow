func foo() : i32
{
    x : i32 = 10;
    x *= 10;
    return x;
}

func bar(x : i32, y : i32) : i32
{
    if (x > y)
        return x;
    else
        return y;
}

foo();
bar(x, y);
bar(10, 20 + 10);
bar(~x, -(10 * 2));
bar(foo(), 1);
x = bar() * 2;